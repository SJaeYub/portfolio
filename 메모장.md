1. MSA NLB 구성
	1. convenience-gateway-nlb, benefit-gateway-nlb, private-gateway-nlb
	2. 각 ns 별로 나뉘어져 있음
	3. 3개 모두 동일한 vpc로 구성되어 있으며 각 nlb는 2개의 가용영역을 갖고있음
	4. 각 ns 별 nlb를 통해 ns 별 istio-gateway로 들어와서 VirtualService에 따라 pod로 이동
2. EKS 버전업
	1. 먼저 각 클러스터의 쉘 스크립트로 해당 노드에 떠있는 Deployment, StatefulSet을 모두 0으로 만듬
	2. aws eks update-cluster-versioin 으로 버전업 진행(1 마이너 버전 씩)
	3. 이후 terraform MR로 control-plane 버전 업그레이드 반영
	4. 서비스 그룹 노드 대수 축소(빠른 적용을 위해)
	5. 변경된 버전 노드 그룹 생성
	6. 그에 맞춰 addon도 버전업 진행
3. EKS 노드 스펙
	1. 서비스 노드: m7i-flex.xlarge
	2. ami: AL2023_x86_64_STANDARD
4. istio 구성
	1. subset을 사용하지 않아 DestinationRule이 없음
	2. 실제 외부 트래픽 시 내부 트래픽 흐름
	   ```
	외부 클라이언트
      │
      ▼
  AWS NLB
  (L4, TCP Passthrough - HTTP 헤더 미해석)
      │
      ▼ ← ① NetworkPolicy 체크 (istio-ingressgateway Pod ingress)
  Istio Ingress Gateway Pod (Envoy)
  - Gateway 리소스: 포트/프로토콜 매칭
  - VirtualService 리소스: 호스트/경로/헤더 기반 라우팅 결정
      │
      ▼ ← ② NetworkPolicy 체크 (백엔드 Pod ingress)
  백엔드 Pod
  - Envoy Sidecar (mTLS 검증, AuthorizationPolicy)
  - App Container

	   ```
	3. mermaid 버전(istio xDS 확인 후 버전)
	   ~~~
	   mermaid
flowchart TB
    EXT(["🌐 외부 클라이언트"])

    subgraph AWS ["☁️ AWS 인프라"]
        NLB["🔀 AWS NLB
        L4 TCP Passthrough
        (HTTP 헤더 미해석)"]
    end

    subgraph ISTIOCTRL ["🧠 istiod (Control Plane)"]
        direction LR
        CDS["CDS
        클러스터 테이블
        (FQDN → 로드밸런싱 정책)"]
        EDS["EDS
        엔드포인트 테이블
        (FQDN → Pod IP 목록)"]
        CDS --> EDS
    end

    subgraph NP1 ["🔒 NetworkPolicy ① 체크 — IngressGateway Pod 경계"]
        subgraph GWPOD ["istio-ingressgateway Pod (Envoy)"]
            GW["Gateway 리소스
            포트 · 프로토콜 매칭"]
            VS["VirtualService 리소스
            Host / Path 기반 라우팅 결정
            destination.host = FQDN"]
            LOOKUP["xDS CDS 조회
            → EDS에서 Pod IP 목록 획득
            → Round Robin으로 1개 선택
            ⚠️ ClusterIP 사용 안 함"]
            GW --> VS --> LOOKUP
        end
    end

    subgraph K8S ["☸️ Kubernetes Cluster  |  Amazon VPC CNI (오버레이 없음, Native VPC 라우팅)"]
        subgraph NP2 ["🔒 NetworkPolicy ② 체크 — 백엔드 Pod 경계"]
            subgraph PODA ["백엔드 Pod A  10.0.1.45"]
                SCA["Envoy Sidecar
                mTLS 검증 · AuthorizationPolicy"]
                APPA["📦 App Container"]
                SCA --> APPA
            end
            subgraph PODB ["백엔드 Pod B  10.0.2.33"]
                SCB["Envoy Sidecar
                mTLS 검증 · AuthorizationPolicy"]
                APPB["📦 App Container"]
                SCB --> APPB
            end
            subgraph PODC ["백엔드 Pod C  10.0.3.11"]
                SCC["Envoy Sidecar
                mTLS 검증 · AuthorizationPolicy"]
                APPC["📦 App Container"]
                SCC --> APPC
            end
        end
    end

    EXT     -->|"① HTTPS / TCP"| NLB
    NLB     -->|"② 직접 Pod IP 도달
    (kube-proxy 우회, IP 타겟 모드)"| NP1
    LOOKUP <-->|"③ xDS push
    (CDS / EDS 동기화)"| ISTIOCTRL
    LOOKUP  -->|"④ CNI 직접 전달 to Pod IP
    ClusterIP DNAT 없음
    Round Robin 분산"| NP2

	   ~~~

5. EKS etcd 백업 보관주기 확인
	1. EKS는 AWS가 직접 etcd 백업을 관리하기 때문에 cloudwatch를 통해서 확인해야함
6. IRSA
	1. 현재 우리의 구조는 다음과 같다
	   ~~~
	AWS secretmanager <- AWS IAM Role&Policy <-> EKS SA -> EKS 내 secret 객체 생성
	                                                            |
	                                                            |
	          istio-vs ------>  eks service ----------> pod 생성 시 해당 secret mount                  |
	              |
	             istio-gateway <- 외부 트래픽
	   
	   
	   
	   ~~~
	   
	1. 